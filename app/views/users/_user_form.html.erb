<!--using devise to create our form-->
<%= simple_form_for @user, as: :user do |f|%> <!-- "as: :user" was add because the update wasn't working without it. Explanation https://stackoverflow.com/questions/45848373/actioncontrollerparametermissing-in-userscontrollerupdate-rails-->
    <div class="form-inputs"  >
        <%= f.input :name %>
        <%= f.input :coordination %>
        <%= f.input :telephone %>
        <%= f.input :email %>
        <%= f.input :company %>
        <%= f.hidden_field :type %> <!--This field must be here. The controller was getting type = nil without it-->
         <!--I put hiden_field because I"m setting the type into the controller-->
    </div>
    <br>
    <div class="form-actions"  >
        <%= f.button :submit, class:"btn btn-primary" %>
        <%= link_to "Back", root_path, class: "btn btn-secondary"%>
    </div>
<%end%>
